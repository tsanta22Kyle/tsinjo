spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/tsinjo_preprod
    username: postgres
    password: secret
    driver-class-name: org.postgresql.Driver
  flyway:
    enabled: true
    locations: classpath:db/migration
